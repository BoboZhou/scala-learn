package com.bobo.scala.common

/**
  * Created by bobo on 2017/7/19.
  */
object Exer {
  def main(args: Array[String]): Unit = {
    val list = List(1, 7, 9, 8, 0, 3, 5, 4, 6, 2)
    //将lst0中每个元素乘以10后生成一个新的集合

    //将lst0中的偶数取出来生成一个新的集合

    //将lst0排序后生成一个新的集合

    //反转顺序

    //将lst0中的元素4个一组,类型为Iterator[List[Int]]

    //将Iterator转换成List

    //将多个list压扁成一个List

    val lines = List("hello tom hello jerry", "hello jerry", "hello kitty")
    //先按空格切分，在压平


    //并行计算求和

    //化简：reduce
    //将非特定顺序的二元操作应用到所有元素

    //安装特点的顺序


    //折叠：有初始值（无特定顺序）

    //折叠：有初始值（有特定顺序）


    //聚合
  }

}
